# Временный override: пробрасываем app_postgres на 5433 чтобы не конфликтовать
# с локальным PostgreSQL на 5432. Удалить файл после использования.
services:
  app_postgres:
    ports:
      - "5434:5432"
